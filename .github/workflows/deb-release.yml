name: Release deb

# Triggers only when files that affect the .deb package change.
# Runs in parallel with docker-publish.yml — each workflow owns its own paths.

on:
  push:
    branches:
      - main
      - master
    paths:
      - launcher
      - docker-compose.yml
      - .env.example
      - package.json
      - scripts/deb/**
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 2

      - name: Get version
        id: version
        run: echo "VERSION=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Build deb
        run: npm run build:deb

      - name: Build changelog body
        id: changelog
        run: |
          files=$(git diff --name-only HEAD~1 HEAD -- 'changelog/*.md' | grep -v README | sort)
          body=""
          for f in $files; do
            [ -f "$f" ] && body="$body$(cat "$f")"$'\n\n'
          done
          if [ -z "$body" ]; then body="No changelog entries for this release."; fi
          {
            echo "BODY<<EOF"
            echo "$body"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ steps.version.outputs.VERSION }}
          name: v${{ steps.version.outputs.VERSION }}
          body: ${{ steps.changelog.outputs.BODY }}
          files: dist/claude-docker_${{ steps.version.outputs.VERSION }}_all.deb
