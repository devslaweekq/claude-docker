name: Docker Publish

# Required repo secrets:
#   DOCKER_USERNAME — Docker Hub login (e.g. slaweekq)
#   DOCKER_PASSWORD — Docker Hub access token (not account password)
#
# Registry cache: slaweekq/claude-docker:buildcache

on:
  push:
    branches: [main, master]
    paths:
      - Dockerfile
      - scripts/builds/update-certs.sh
      - scripts/builds/ensure-claude-cli.sh
      - scripts/builds/menu.sh
      - scripts/builds/sessions.js
      - claude-defaults/**
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build
        run: bash ./scripts/docker/build.sh --claude

      - name: Push
        run: bash ./scripts/docker/push.sh --claude
