# Secrets and runtime state — do not commit
.env
certs/*.crt
certs/*.key

# Web console frontend vendor assets — fetched at Docker build time, not committed
claude-docker/web/vendor/

# Editor junk
*.swp
*.swo
*~
.DS_Store

# Windows: generated icon/shortcut
claude.ico
.claude/worktrees
**/.fuse_*
*.lnk

.omc

dist
build
